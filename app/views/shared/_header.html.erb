<%= render :partial => "shared/logo" %>

<div id="user_status_header">
	Welcome <%= current_user ? current_user.email : "Guest" %> , 
	
	<% if user_signed_in? %>
	  <%= link_to "log out" , destroy_user_session_path %>	
	<% else %>
	  <%= link_to "login", new_user_session_path %>
	<% end %>
	
</div>